# Examples

Some appendices files (`*.zip`, `*.pptx`, `*.docx`, etc.) are stored with [Git LFS](https://git-lfs.com/).

If you clone this repository locally, please install Git LFS and run `git lfs pull` to download these files.
